{
    "layer": "top",
    "position": "bottom",
    "height": 44,
    "modules-left": ["custom/launcher"],
    "modules-center": ["wlr/taskbar"],
    "modules-right": ["custom/quicksettings", "battery", "clock", "tray"],

    "custom/launcher": {
        "format": "🐤",
        "on-click": "/usr/local/bin/canary-launcher",
        "tooltip": false
    },

    "wlr/taskbar": {
        "format": "{icon}",
        "icon-size": 24,
        "tooltip-format": "{title}",
        "on-click": "activate",
        "on-click-middle": "close",
        "ignore-list": ["wofi", "canary-control-center"],
        "app_ids-mapping": {
            "firefox-esr": "firefox"
        }
    },

    "custom/quicksettings": {
        "format": "   ", 
        "on-click": "/usr/local/bin/canary-toggle-control",
        "tooltip": false
    },

    "battery": {
        "format": "{icon} {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },

    "clock": {
        "format": "{:%H:%M}",
        "tooltip-format": "{:%d-%m-%Y}",
	"on-click": "swaync-client -t -sw"
    },

    "tray": {
        "spacing": 10
    }
}
